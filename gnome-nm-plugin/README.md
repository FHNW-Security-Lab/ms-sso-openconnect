# Legacy GNOME Plugin Path

This directory is kept for compatibility with older commands and docs.

Canonical implementation moved to:

- `frontends/gnome-plugin/`

Compatibility command:

```bash
./gnome-nm-plugin/build-deb.sh
./gnome-nm-plugin/install-dev.sh
```
